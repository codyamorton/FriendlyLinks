@model IEnumerable<FriendlyLinks.Data.TeeTime>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GolfCourse.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CoursePrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeeOffTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GolfCourse.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CoursePrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeeOffTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeeTimeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeeTimeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeeTimeId })
        </td>
    </tr>
}
</table>